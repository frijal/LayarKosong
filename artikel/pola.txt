const shareTwitter = document.getElementById('share-twitter');
        if (shareTwitter) {
          shareTwitter.href = `https://twitter.com/intent/tweet?url=${encodedUrl}&text=${encodedTitle}`;
          shareTwitter.target = '_blank';
          shareTwitter.rel = 'noopener noreferrer';
        }
        const shareFacebook = document.getElementById('share-facebook');
        if (shareFacebook) {
          shareFacebook.href = `https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`;
          shareFacebook.target = '_blank';
          shareFacebook.rel = 'noopener noreferrer';
        }
        const shareLinkedIn = document.getElementById('share-linkedin');
        if (shareLinkedIn) {
          shareLinkedIn.href = `https://www.linkedin.com/shareArticle?mini=true&url=${encodedUrl}&title=${encodedTitle}`;
          shareLinkedIn.target = '_blank';
          shareLinkedIn.rel = 'noopener noreferrer';
        }
        const shareWhatsApp = document.getElementById('share-whatsapp');
        if (shareWhatsApp) {
          shareWhatsApp.href = `whatsapp://send?text=${encodedTitle}%20${encodedUrl}`;
          shareWhatsApp.target = '_blank';
        }
