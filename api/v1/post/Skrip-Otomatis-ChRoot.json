{
  "id": "Skrip-Otomatis-ChRoot",
  "slug": "Skrip-Otomatis-ChRoot",
  "title": "Skrip Otomatis: Setup & Cleanup chroot",
  "category": "🐧 Linux & Open Source",
  "published_at": "2025-10-14T18:15:06.378+00:00",
  "url": "https://dalam.web.id/artikel/Skrip-Otomatis-ChRoot.html",
  "image": "https://dalam.web.id/img/Skrip-Otomatis-ChRoot.webp",
  "content": {
    "text": "🛠️ Skrip Otomatis: Setup & Cleanup chroot Untuk memudahkan, berikut contoh skrip shell sederhana agar tidak perlu mengetik semua perintah mount/umount secara manual. 1. Setup (buat file enter-chroot.sh) Copy#!/bin/bash # enter-chroot.sh # Gunakan: sudo ./enter-chroot.sh /srv/chroot/debian CHROOT_DIR=\"$1\" if [ -z \"$CHROOT_DIR\" ]; then echo \"Usage: $0 /path/to/chroot\" exit 1 fi # Mount filesystem penting mount --bind /dev \"$CHROOT_DIR/dev\" mount --bind /dev/pts \"$CHROOT_DIR/dev/pts\" mount -t proc /proc \"$CHROOT_DIR/proc\" mount -t sysfs /sys \"$CHROOT_DIR/sys\" # Copy resolv.conf untuk DNS cp /etc/resolv.conf \"$CHROOT_DIR/etc/\" # Masuk ke chroot chroot \"$CHROOT_DIR\" /bin/bash 2. Cleanup (buat file exit-chroot.sh) Copy#!/bin/bash # exit-chroot.sh # Gunakan: sudo ./exit-chroot.sh /srv/chroot/debian CHROOT_DIR=\"$1\" if [ -z \"$CHROOT_DIR\" ]; then echo \"Usage: $0 /path/to/chroot\" exit 1 fi umount \"$CHROOT_DIR/dev/pts\" umount \"$CHROOT_DIR/dev\" umount \"$CHROOT_DIR/proc\" umount \"$CHROOT_DIR/sys\" Pastikan memberi izin eksekusi: Copychmod +x enter-chroot.sh exit-chroot.sh Sekarang kamu cukup menjalankan: Copysudo ./enter-chroot.sh /srv/chroot/debian # ...kerja di dalam chroot... exit sudo ./exit-chroot.sh /srv/chroot/debian Dengan cara ini, mount/umount jadi lebih aman dan konsisten. Skrip ini cocok untuk berbagai distro Debian/Ubuntu yang menggunakan chroot untuk testing, recovery, atau pembangunan paket."
  },
  "meta": {
    "summary": "Panduan lengkap membuat skrip otomatis untuk setup dan cleanup chroot di Linux. Praktis, aman, dan hemat waktu.",
    "keywords": [],
    "topics": [],
    "prompt_hint": "Panduan lengkap membuat skrip otomatis untuk setup dan cleanup chroot di Linux. Praktis, aman, dan hemat waktu."
  }
}