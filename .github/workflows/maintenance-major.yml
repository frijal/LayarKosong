name: 🚨 Major Dependency Upgrade (Scheduled & Manual)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 1 */3 *' # Tiap 3 bulan sekali

jobs:
  major-upgrade:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: 🧩 Checkout kode
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: '2. Setup Bun.js'
        uses: oven-sh/setup-bun@3d267786b128fe76c2f16a390aa2448b815359f3 # v2
        with:
          bun-version: latest

      - name: 📦 Install Awal
        run: bun install

      - name: 🔍 Preview & Apply Major Upgrades
        run: |
          bun install -g npm-check-updates
          echo "--- DAFTAR UPGRADE MAJOR ---"
          ncu --target latest --format group
          
          # Langsung hajar update ke package.json
          ncu -u --target latest

      - name: 🧹 Menjalankan Tukang Sapu (Cleanup)
        run: |
          if [ -f ext/cleanup.js ]; then
            echo "🔥 Membersihkan node_modules dan membangun ulang lockfile..."
            bun run ext/cleanup.js
          else
            bun install
          fi

      - name: 🕵️ Detektif Scan (Cek paket yang mati setelah upgrade)
        run: |
          if [ -f ext/cek-mubazir.js ]; then
            echo "--- LAPORAN DETEKTIF SETELAH UPGRADE ---"
            bun run ext/cek-mubazir.js
          fi

      - name: 🧪 Audit & Test
        run: |
          # Menggunakan bunx untuk menjalankan audit tanpa install permanen
          bunx audit-ci --low || true
          
          # Menggunakan bun test (fitur test runner bawaan Bun yang super cepat)
          bun test || echo "No tests found, skipping..."

      - name: 🔍 Cek perubahan final
        id: check_changes
        run: |
          if [[ -n "$(git status --porcelain package.json package-lock.json)" ]]; then
            echo "changes=true" >> $GITHUB_OUTPUT
          else
            echo "changes=false" >> $GITHUB_OUTPUT
          fi

      - name: 📨 Buat Pull Request
        if: steps.check_changes.outputs.changes == 'true'
        uses: peter-evans/create-pull-request@c0f553fe549906ede9cf27b5156039d195d2ece0 # v8
        with:
          branch: chore/major-upgrade-${{ github.run_id }}
          title: 'chore: 🚨 Major Dependency Upgrade'
          commit-message: 'chore: upgrade dependencies to latest major versions'
          body: |
            🚨 **Operasi Major Upgrade Berhasil!**

            Perubahan yang dilakukan:
            - Semua paket di-upgrade ke versi **Latest/Major**.
            - `ext/cleanup.js` telah membersihkan dan mengoptimalkan `node_modules`.
            - `ext/cek-mubazir.js` telah memverifikasi penggunaan dependensi.

            **Catatan Penting:** Upgrade versi Major seringkali membawa *breaking changes*. Mohon cek kembali fungsi-fungsi krusial di blog **Layar Kosong** sebelum melakukan merge.
          labels: dependencies, major, breaking-change
          reviewers: frijal
          draft: false

      - name: ℹ️ Tidak ada perubahan
        if: steps.check_changes.outputs.changes == 'false'
        run: echo "💤 Semua paket sudah berada di versi paling baru."
