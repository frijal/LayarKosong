name: ☢️ Build and Generate Site Files

on:
  workflow_run:
    workflows: ["🔄 Proses ArtikelX"]
    types: [completed]
    branches: ['main']
  workflow_dispatch:
    inputs:
      run_generator:
        description: 'Jalankan Generator (JSON, RSS, Sitemap XML)'
        type: boolean
        default: true
      run_sitemap_txt:
        description: 'Jalankan Update sitemap.txt'
        type: boolean
        default: true
      run_schema:
        description: 'Jalankan Inject Schema.org Python'
        type: boolean
        default: true
      run_minify_html:
        description: 'Jalankan HTML Minify'
        type: boolean
        default: true
      run_minify_jsonxml:
        description: 'Jalankan JSONXML Minify'
        type: boolean
        default: true
      run_convert_markdown:
        description: 'ubah ke format markdown'
        type: boolean
        default: true  

env:
  DO_GENERATOR: ${{ github.event.inputs.run_generator || 'true' }}
  DO_SITEMAP_TXT: ${{ github.event.inputs.run_sitemap_txt || 'true' }}
  DO_SCHEMA: ${{ github.event.inputs.run_schema || 'true' }}
  DO_MINIFY_HTML: ${{ github.event.inputs.run_minify_html || 'true' }}
  DO_MINIFY_JSONXML: ${{ github.event.inputs.run_minify_jsonxml || 'true' }}
  DO_CONVERT_MARKDOWN : ${{ github.event.inputs.run_convert_markdown || 'true' }}

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: write

    steps:
      - name: '1. Checkout Repository'
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.ref }}
          fetch-depth: 1
          persist-credentials: true

      - name: Cache Rust
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Node Modules
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: '2. Setup Node.js'
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6.2.0
        with:
          node-version: '24'

      - name: '3. Install Dependencies'
        run: npm ci

      - name: '4. Setup Python' # Dibutuhkan untuk step Schema nanti
        if: env.DO_SCHEMA == 'true'
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: '3.x'

    #  - name: '5️⃣ Generate artikel.json, Sitemap XML & RSS'
    #    if: env.DO_GENERATOR == 'true'
    #    run: npm run ci-generate-xml-rss
    #    env:
    #      RSS_LIMIT: 30

      - name: '5️⃣ Generate artikel.json, Sitemap XML & RSS (Rust Engine)'
        if: env.DO_GENERATOR == 'true'
        run: cargo run --release --bin generator-pro

      - name: '6️⃣ Generate sitemap.txt'
        if: env.DO_SITEMAP_TXT == 'true'
        run: |
          echo "📝 Membuat sitemap.txt (Clean URL)..."
          BASE_URL="https://dalam.web.id"
          node -e "
            const fs = require('fs');
            const data = JSON.parse(fs.readFileSync('artikel.json', 'utf8'));
            const urls = [];
            const slugify = (t) => t.toLowerCase().trim().replace(/\s+/g, '-');

            // 1. Ambil URL Artikel & Kategori (Tanpa Slash)
            for (const [cat, posts] of Object.entries(data)) {
              const catSlug = slugify(cat);
              posts.forEach(p => {
                const fileSlug = p[1].replace('.html', '').replace(/^\//, '');
                // 🔥 Hapus / di akhir URL artikel
                urls.push('${BASE_URL}/' + catSlug + '/' + fileSlug);
              });
            }

            // 2. Daftar URL Manual (Tanpa Slash di akhir)
            // Khusus root '/', tetap dibiarkan '/' atau kosong tergantung selera, 
            // tapi di sini saya buat konsisten ke root domain.
            [
              '', '/jejak-sejarah', '/lainnya', '/olah-media', 
              '/opini-sosial', '/sistem-terbuka', '/warta-tekno', 
              '/gaya-hidup', '/about', '/privacy', '/search', 
              '/security-policy', '/data-deletion-form', '/disclaimer', 
              '/feed', '/img', '/lisensi', '/sitemap'
            ].forEach(u => urls.push('${BASE_URL}' + u));

            // 3. Tulis file dengan URL unik
            fs.writeFileSync('sitemap.txt', [...new Set(urls)].join('\n') + '\n');
          "
     
      - name: '7️⃣ Inject schema.org (BARU KEMUDIAN)'
        if: env.DO_SCHEMA == 'true'
        run: cargo run --release --bin inject-schema

      - name: 📝 Convert HTML Tags to Markdown (Rust)
        if: env.DO_CONVERT_MARKDOWN == 'true'
        run: cargo run --release --bin html-to-markdown
        
      - name: '8️⃣ Clean HTML Cerdas (rust-html-minify)'
        if: env.DO_MINIFY_HTML == 'true'
        run: node ext/minify-html.js

      - name: 🧹 Minify Data (JSON & XML)
        if: env.DO_MINIFY_JSONXML == 'true'
        run: node ext/minify-jsonxml.js     

      - name: '9️⃣ Commit and Push'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --staged --quiet; then
            echo "✅ Tidak ada perubahan."
          else
            git commit -m "build: 🚀 site assets (webp mirrors, json, rss, sitemap)"
            git pull --rebase origin main
            git push origin main
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
