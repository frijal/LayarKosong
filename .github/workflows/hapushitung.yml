name: 🔆 Pengecekan & Laporan Konten Harian

on:
  schedule:
    - cron: '0 11 * * *' # Jam 11 UTC / 19:00 WITA
  workflow_dispatch: # Bisa dijalankan manual dari tab Actions

jobs:
  maintenance_and_reporting:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # ========== SETUP ENVIRONMENT ==========
      - name: 1. Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1
          persist-credentials: true

      - name: 🚀 Setup Bun.js
        uses: oven-sh/setup-bun@3d267786b128fe76c2f16a390aa2448b815359f3 # v2
        with:
          bun-version: latest

      - name: 📂 Cache Bun Dependencies
        uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-cache-static
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: 🛠️ Maintenance & Update
        run: |
          bun update --latest
          bun install 

      # ========== AUTOMATION SCRIPTS ==========
      - name: Jalankan Koki (Mode Sekali Jalan)
        run: bun run dapur/koki.ts --once
      
      - name: 🕵️ Update Dashboard Jadwal
        run: bun run dapur/workflow-list.js || echo "Script dashboard tidak ditemukan, skip."

      - name: 📸 Scan Unused Images
        run: |
          echo "🔎 Menjalankan pengecekan gambar..."
          bun run dapur/cek-gambar.js || echo "Script cek-gambar tidak ditemukan, skip."

      # ========== CONTENT REPORT GENERATION (ANTI-ERROR V7.0) ==========
      - name: 📊 Generate Detailed Content Report
        id: generate_report
        run: |
          echo "📊 Membuat laporan markdown dinamis V6.9 (Auto-detect Categories)..."

          # 1. Setup Awal
          DATE=$(date +'%Y-%m-%d')
          REPORT_PATH="mini/${DATE}-laporan_konten.md"
          MAX_BLOCKS=20

          # Emoji mapping
          declare -A ext_emoji=(
            [css]="🎨" [html]="🌐" [ico]="🔑" [jpeg]="🖼️" [jpg]="🖼️"
            [js]="📜" [json]="🗂️" [png]="🖼️" [py]="🐍" [sh]="💻"
            [svg]="✏️" [txt]="📄" [webp]="🖼️" [md]="📝" [no-ext]="❓"
          )

          # === DETEKSI FOLDER DINAMIS ===
          SKIP_PATTERN="^\./(\.git|\.github|node_modules|mini)$"
          # Ambil folder dan bersihkan prefix ./
          find . -maxdepth 2 -type d -not -path '*/.*' | grep -vE "$SKIP_PATTERN" | sed 's|^\./||' | sort -u > folders.txt

          declare -A global_ext_count
          declare -A global_ext_folders
          max_global_count=0
          total_global=0

          # 2. Pengumpulan Statistik Global & Lokasi
          while read -r folder; do
            [[ -z "$folder" || "$folder" == "." ]] && continue
            while IFS= read -r -d $'\0' file; do
              ext="${file##*.}"
              [[ "$file" != *.* ]] && ext="no-ext"
              
              global_ext_count["$ext"]=$(( ${global_ext_count["$ext"]:-0} + 1 ))
              dir_path=$(dirname "$file")
              
              # Catat folder unik untuk setiap ekstensi
              if [[ ! "${global_ext_folders["$ext"]}" =~ "$dir_path" ]]; then
                global_ext_folders["$ext"]+="$dir_path "
              fi
            done < <(find "$folder" -maxdepth 2 -type f -print0 2>/dev/null)
          done < folders.txt

          # Cari nilai maksimal untuk scaling histogram
          for c in "${global_ext_count[@]}"; do
            (( c > max_global_count )) && max_global_count=$c
          done
          (( max_global_count == 0 )) && max_global_count=1

          # 3. Generate Body Laporan
          {
            echo "# 🔆 Laporan Konten Harian Layar Kosong"
            echo "Dihasilkan pada: $(date '+%d %B %Y %H:%M:%S') Balikpapan Time"
            echo ""

            # Analisis Per Folder
            while read -r folder; do
              [[ -z "$folder" || "$folder" == "." ]] && continue
              
              # Scan subfolder termasuk dirinya sendiri
              find "$folder" -maxdepth 2 -type d -print0 2>/dev/null | while IFS= read -r -d $'\0' subfolder; do
                declare -A subf_count
                has_files=false
                
                while IFS= read -r -d $'\0' file; do
                  ext="${file##*.}"
                  [[ "$file" != *.* ]] && ext="no-ext"
                  subf_count["$ext"]=$(( ${subf_count["$ext"]:-0} + 1 ))
                  has_files=true
                done < <(find "$subfolder" -maxdepth 1 -type f -print0 2>/dev/null)

                if [ "$has_files" = true ]; then
                  echo "### 📂 Folder: $subfolder"
                  echo ""
                  echo "| Jenis File | Jumlah | Histogram |"
                  echo "|:-----------|-------:|:----------|"

                  for ext in $(printf "%s\n" "${!subf_count[@]}" | sort); do
                    count=${subf_count["$ext"]}
                    total_global=$(( total_global + count ))
                    
                    blocks=$(( (count * MAX_BLOCKS + max_global_count - 1) / max_global_count ))
                    [[ $blocks -gt $MAX_BLOCKS ]] && blocks=$MAX_BLOCKS
                    histogram=$(printf '▇%.0s' $(seq 1 "$blocks" 2>/dev/null || echo 1))
                    emoji=${ext_emoji["$ext"]:-📄}
                    
                    printf "| %s %-8s | %6d | %s |\n" "$emoji" "$ext" "$count" "$histogram"
                  done
                  echo ""
                fi
                unset subf_count
              done
            done < folders.txt

            # 4. Ringkasan Global
            echo "---"
            echo "## 📊 Ringkasan Statistik Global"
            echo ""
            echo "| Ekstensi | Total | Histogram | Lokasi |"
            echo "|:---------|------:|:----------|:-------|"

            for ext in $(printf "%s\n" "${!global_ext_count[@]}" | sort); do
              count=${global_ext_count["$ext"]}
              blocks=$(( (count * MAX_BLOCKS + max_global_count - 1) / max_global_count ))
              [[ $blocks -gt $MAX_BLOCKS ]] && blocks=$MAX_BLOCKS
              histogram=$(printf '▇%.0s' $(seq 1 "$blocks" 2>/dev/null || echo 1))
              emoji=${ext_emoji["$ext"]:-📄}
              
              # Merapikan list folder unik
              folder_unique=$(echo "${global_ext_folders["$ext"]}" | tr ' ' '\n' | grep -v "^$" | sort -u | tr '\n' ',' | sed 's/,$//; s/,/, /g')
              
              printf "| %s %-8s | %6d | %s | %s |\n" "$emoji" "$ext" "$count" "$histogram" "$folder_unique"
            done
            echo "| **TOTAL** | **$total_global** | | |"
          } > "$REPORT_PATH"

          echo "✅ Laporan V6.9 sukses: $REPORT_PATH"
          rm folders.txt
          
      # ========== COMMIT & PUSH ==========
      - name: 📮 Commit & Push All Changes
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            echo "📝 Perubahan ditemukan, melakukan commit..."
            
            git config --global user.name 'github-actions[bot]'
            git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
            
            git add .
            
            DATE=${{ env.REPORT_DATE }}
            added_count=$(git diff --staged --name-status | grep -c "^A" || true)
            deleted_count=$(git diff --staged --name-status | grep -c "^D" || true)
            
            # [skip ci] sangat penting untuk mencegah infinite loop!
            git commit -m "chore: Daily maintenance report ($DATE) 📊 [skip ci]

            - Added: $added_count file(s)
            - Deleted: $deleted_count file(s)
            - Report: ${DATE}-laporan_konten.md" \
            --author="frijal <5477182+frijal@users.noreply.github.com>"
            
            git pull origin main --rebase --strategy-option=theirs
            git push origin main
            echo "✅ Sukses di-push ke main!"
          else
            echo "✅ Tidak ada perubahan yang perlu di-commit."
          fi
