name: 📋 Audit Komponen SEO Lengkap

on:
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🧩 Checkout kode
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install BeautifulSoup
        run: pip install beautifulsoup4

      - name: 🔍 Jalankan Audit SEO (Python)
        id: run_audit
        run: |
          # Pastikan folder mini tersedia
          mkdir -p mini
          
          # Eksekusi skrip audit Python yang sudah mendukung <details> dan BS4
          python ext/audit_seo.py
          
          # Ambil file laporan terbaru untuk Summary
          LATEST_REPORT=$(ls -t mini/laporan-audit-*.md | head -1)
          cat "$LATEST_REPORT" >> $GITHUB_STEP_SUMMARY
          echo "REPORT_FILE=$LATEST_REPORT" >> $GITHUB_OUTPUT

      - name: 💾 Simpan Laporan ke Repo
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add mini/*.md
          
          # Commit hanya jika ada file baru atau perubahan
          if ! git diff --cached --quiet; then
            git commit -m "🤖 docs: audit SEO (Python + Details Format) $(date +'%Y-%m-%d')"
            git push
          else
            echo "💤 Tidak ada perubahan pada laporan audit."
          fi
